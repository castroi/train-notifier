node_modules
dist
*.log

# Secrets / local config — never commit (public repo)
.env
.env.*
!.env.example
config.yaml
config.yaml.bak.*
*.bak

# Local-only scripts / helpers — never committed
pi-scripts/

# per-bot wrapper token (docker secret) — never commit
secrets/
